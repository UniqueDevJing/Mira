# Mira 评测报告

> 所有数字均来自仓库内可复跑脚本的真实输出（生成日期 2026-09-05）。复现命令见每节末尾。

## 1. 检索质量（390 问离线评测，1531 chunk 语料）

生产配置 = 融合检索 + 自适应加权（α=0.9）+ rerank。对照组为纯向量召回与 rerank-only：

| 配置 | Recall@1 | Recall@3 | Recall@5 | Recall@10 | Hit@1 | MRR |
|---|---|---|---|---|---|---|
| 纯向量召回（rerank off） | 0.347 | 0.740 | 0.891 | 0.979 | 0.536 | 0.743 |
| rerank-only（无融合） | 0.349 | 0.711 | 0.883 | 0.979 | 0.521 | 0.732 |
| **融合 + 自适应（生产配置）** | **0.390** | **0.746** | **0.886** | **0.979** | **0.592** | **0.772** |

要点：
- 融合+自适应相对纯向量 **Recall@1 +4.3pp、MRR +0.029**，验证融合修复必要性；
- rerank 单独使用在 Recall@3 上回退（0.711 < 0.740），必须与融合配合使用；
- RRF 首阶段漏召仅 3/390（0.8%），rerank 翻盘率 0%（漏召天花板已触顶）；
- 4 个 hard 查询（跨库歧义/实体消歧）全部由跨库扇出覆盖。

## 2. KB 级生产对齐评测（n=80，10 知识库逐库）

用各知识库真实入库语料构建问题、走完整生产路由+检索链路：

| 指标 | 数值 |
|---|---|
| MRR | **0.815** |
| Recall@1 | 0.713 |
| Recall@3 | 0.900 |
| Recall@5 | 0.938 |
| Recall@10 | **0.975** |

## 3. 路由准确率（80 题 × 10 知识库，LLM-as-judge 之外的真实路由）

`tests/eval_dataset_kb.json`（10 库 × 8 题）走线上同款 IntentRouter（规则 + LLM 兜底）：

| 口径 | 准确率（超时 1.5s） | 准确率（超时 3.0s，当前默认） |
|---|---|---|
| Top-1 命中 | 56.2% | **58.8%** |
| Top-2 命中（扇出口径） | 71.2% | **73.8%** |

- 来源分布（3.0s）：rule 34 / llm 46 / fallback 0 —— 原阈值 1.5s 下 p95 兜底调用 1.39s 贴线，高峰误降级约 30%，放宽后消除并免费 +2.6pp；
- 错路由集中在跨库歧义主题（退款类在 policy/service 两库均有内容，黄金标签本身存在歧义）；跨库题由 selective fanout 双库并行检索兜住；
- 路由延迟：平均 627ms（LLM 兜底题），规则命中题 <10ms。

另：13 道黄金路由题作为 CI 回归门禁（`make eval-routing`），持续通过。

## 3.5 端到端答案准确率（390 问，LLM-as-judge，2026-09-06 新增）

链路：与线上一致的检索（向量+BM25+RRF+重排）→ LLM 生成 → LLM 评审员对比标准答案判分。

| 指标 | 数值 |
|---|---|
| 完全正确（correct） | **44.9%**（175/390） |
| 部分正确（partial） | 43.9%（171/390） |
| **可用率（correct + partial）** | **88.8%** |
| 错误（incorrect） | 8.2%（32/390） |
| 拒答（refusal） | 2.8%（11/390，均为片段不足时的正确兜底行为） |
| 无支撑断言率（严格口径） | 36.9% |
| 平均单题延迟 | 2.46s（生成+评审） |

口径说明：
- "无支撑断言率 36.9%" 是**严格评审口径**——回答中含标准答案之外的任何具体事实断言（含客服式补充、关联提示）都计入，不等于"错误事实率"；真实错误率为 incorrect 8.2%；
- 提升方向：partial 43.9% 的"部分正确"主要因回答完整度不足，可通过生成 prompt 强化"覆盖标准答案全部要点"优化。

## 4. 幻觉率 / 忠实度护栏量化（n=1269）

对"数字型幻觉"（LLM 篡改知识库中的数字——客服场景最高危幻觉类型）的量化：

| 指标 | 数值 |
|---|---|
| 正确答案误拒率（false-reject） | **0.0%**（0/1269，其中含数字答案 804 条） |
| 幻觉引入新数字的样例 | 84 条 |
| 数字校验护栏检出率 | **100%**（84/84） |
| 仅靠词重合（关闭数字校验）检出率 | 19.0% |
| 数字校验独有增量 | **81.0%** |

结论：数字校验护栏在不误伤任何正确答案的前提下，将数字型幻觉检出率从 19% 提升到 100%。

## 5. 拒答护栏误拒量化（preguard）

生成前"零词重合"拒答护栏的三组压力测试（语义下限 emb_sim 阈值扫描）：

| 组 | n | 词重合误拒 | 语义下限 0.5 误拒 |
|---|---|---|---|
| G1-eval390（390 正常问题） | 390 | 0 | 0 |
| G2-hard（390 困难改写） | 390 | 0 | 0 |
| G3-para（10 同义改写） | 10 | 9 → 语义兜底后 | 见下 |

- 正常问题与困难改写：**零误拒**；
- 同义改写组 10 例中 9 例词重合为零——语义下限兜底（emb_sim ≥ 0.5 放行）将其中的误拒消除，正确答案召回不受损。

## 6. rerank 融合权重调优（alpha sweep，2026-09-06）

对 390 问全量做 CE 分数缓存 + alpha 网格重排（`scripts/tune_rerank_alpha.py`，近似复现生产融合链路）：

| 配置 | R@1 | R@3 | R@5 | R@10 | MRR |
|---|---|---|---|---|---|
| alpha=0.5 | 0.317 | 0.697 | 0.867 | 0.977 | 0.709 |
| alpha=0.7 | 0.314 | 0.706 | 0.871 | 0.978 | 0.710 |
| **alpha=0.9（生产默认）** | **0.315** | **0.712** | **0.873** | **0.982** | **0.713** |
| alpha=1.0（纯 CE） | **0.350** | 0.708 | 0.878 | 0.981 | **0.733** |

结论（负结果也是结论）：
- **alpha=0.9 已是 R@3 最优**——降低 alpha（加重 RRF 托底）反而降 R@3；alpha 维度无免费提升；
- alpha=1.0（纯 CE 分）R@1/MRR 更高但 R@3 略降，不构成切换理由。

### 6.1 候选深度 sweep（负结果）

candidate_k ∈ {5,10,15,20,25}（alpha=0.9 固定，`scripts/tune_rerank_depth.py`）：**R@1/R@3/MRR 随深度单调下降**（R@3: 0.715→0.689），仅 R@10 随深度上升（0.889→0.961）。当前生产 candidate_k=10 是 precision-recall 的合理平衡点，候选深度维度无优化空间。

### 6.2 CE 输入长度 sweep（正收益，已应用）

max_length 256 vs 384（candidate_k=10，alpha=0.9，`scripts/tune_rerank_maxlen.py`）：

| max_length | R@1 | R@3 | R@5 | R@10 | MRR |
|---|---|---|---|---|---|
| 256（旧默认） | 0.330 | 0.700 | 0.866 | 0.978 | 0.713 |
| **384（已应用）** | 0.330 | **0.715** | **0.871** | 0.978 | **0.722** |

**R@3 +1.45pp、MRR +0.86pp，纯排序精度提升**（R@1/R@10 不变）。代价 CE 推理延迟 +62%（595→964ms），但 rerank 走后台异步（stream_sources_before_rerank），首字可见不受影响。`reranker_max_length` 默认值已更新为 384。

## 7. RAGAS 评测（389 样本，2026-09-06）

按 RAGAS 论文 (Es et al. 2023) 定义自实现四指标（`scripts/eval_ragas_metrics.py`；判定模型 qwen-plus，相似度 bge-small-zh；ragas 官方包因依赖链与 Python 3.14 不兼容未采用）：

| 指标 | 数值 | 含义 |
|---|---|---|
| **faithfulness** | **0.914** | 答案断言被检索上下文支持的比例（反幻觉） |
| answer_relevancy | 0.749 | 答案与问题的相关程度 |
| context_recall | 0.868 | 标准答案被检索上下文覆盖的比例 |
| context_precision | 0.571 | 检索上下文对答案的有用度（rank 加权） |

交叉印证：faithfulness 91.4% 与数字型幻觉检出 100% + 端到端可用率 88.8% 一致；context_precision 57% 与 R@1 0.39、路由 top1 58.8% 同源——**检索排序精度是当前系统性短板**，recall 已触顶而 precision 未跟上，指向 rerank 排序质量的下一轮优化。

## 8. 运行时近似指标（每次问答实时产出）

`QAResponse.qa_metrics` 提供近似 faithfulness / retrieval_relevance / confidence（词重合 + 语义相似度近似，非 LLM 评审），用于在线监控与告警，不作为离线结论。

## 9. 局限与后续

- ~~端到端答案准确率~~ ✅ 已纳入（见 3.5 节，LLM-as-judge 390 题全量）；
- 幻觉量化目前覆盖数字型幻觉；陈述型幻觉依赖 faithfulness 近似指标 + 词重合护栏，覆盖不完全；
- 路由 top1 58.8% 的主要瓶颈是跨库歧义主题（评测集黄金标签在 policy/service 间存在双重归属），扇出机制已兜底 top2 73.8%；进一步提升需人工澄清歧义标注或引入主题级路由规则；
- 评测语料（19MB 派生数据）不入库，基线文件随代码版本化。

## 复现

```bash
make eval-routing        # 路由回归 (13 题)
python scripts/eval_retrieval.py --fusion --adaptive   # 390 问检索
python scripts/eval_kb_retrieval.py                    # KB 级 n=80
python scripts/eval_preguard.py                        # 拒答误拒量化
python scripts/eval_fidelity_guardrail.py              # 数字型幻觉量化
```
