apiVersion: 1

providers:
  - name: "RAG 2.0"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      # 挂载目录内含 grafana-dashboard.json (由本仓库维护)
      path: /var/lib/grafana/dashboards
